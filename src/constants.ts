// STAGE_CHECKLISTS foi descontinuado em favor da configuração dinâmica carregada do banco de dados (tabela kanban_checklist_templates).
// Mantendo export vazio para evitar quebras em imports remanescentes não detectados, mas o ideal é remover o uso.
export const STAGE_CHECKLISTS: { [key: string]: string[] } = {};

